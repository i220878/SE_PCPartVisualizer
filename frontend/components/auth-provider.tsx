"use client"

import React from "react"

// This is a placeholder auth provider
// You'll want to replace this with your actual authentication logic
export function AuthProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}

export default AuthProvider